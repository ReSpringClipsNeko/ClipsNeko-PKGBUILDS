post_install() {
  if usr/bin/pacman-key -l >/dev/null 2>&1; then
    usr/bin/pacman-key --populate clipsneko
  else
    echo 'The pacman keyring is not initialized.' >&2
    echo 'Run pacman-key --init, then pacman-key --populate clipsneko.' >&2
  fi
}

post_upgrade() {
  post_install
}
